include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - lib/__gen__/**
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - build/**
    - android/**
    - ios/**
    - web/**

linter:
  rules:
    prefer_relative_imports: true
